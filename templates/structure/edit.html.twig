{% extends 'base.html.twig' %}

  {% block head %}
    <title>{% block title %}URHAJ Normandie | Modification de structure{% endblock %}</title>
    {% block icon %}<link rel='icon' href='/img/icone.ico'>{% endblock %}
  {% endblock %}

{% block body %}
    <style>
        body{
            background-color: #14182A;
        }
    </style>
        <br><br><br><br>
    <div align='center'>
        <div class="title">Modifiez la structure</div>
        <div class="subtitle">Remplissez les informations nécéssaires</div>
    </div>

    {{ include('structure/_form.html.twig', {'button_label': 'Update'}) }}

    {# <a href="{{ path('app_structure_index') }}">back to list</a> #}

    {# {{ include('structure/_delete_form.html.twig') }} #}
{% endblock %}
